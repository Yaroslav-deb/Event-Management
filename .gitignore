# Залежності (спрацює і для backend, і для frontend)
node_modules/

# Змінні оточення (НАЙВАЖЛИВІШЕ - щоб не злити паролі в інтернет)
.env
.env.*
!.env.example

# Збірка фронтенду (те, що генерує Vite при деплої)
dist/
dist-ssr/
*.local

# Логи помилок
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Системні файли ОС
.DS_Store
Thumbs.db